# run start up prompt
$HOME/.bashfuncs/info-startup-prompt

# set vim key bindings
set -o vi

# add .bashfuncs to path
PATH=$PATH:$HOME/.bashfuncs

# add .sec to path
PATH=$PATH:$HOME/.sec

# export path
export PATH=$PATH:/root/.local/bin

